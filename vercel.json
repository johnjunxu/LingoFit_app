{
  "builds": [
    {
      "src": "/api/**/*.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["../../.env*.local"]
      }
    }
  ],
  "env": {
    "GEMINI_API_KEY": "@gemini_api_key"
  }
}
